type UserID = string | number;

enum Role {
    admin,
    empleado,
    trabajador
}

interface IUser {
    id: number | string,
    name: string,
    age: number
}

class User {
   constructor(id, name, age){
    this.IUser.id,
   }
}